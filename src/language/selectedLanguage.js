/*
import { useSelector } from 'react-redux';
import en from './En.js';
import ar from './An.js';

const { settings } = useSelector((state) => state.alchemy);
  let obj = settings;
let Slg={};
switch (obj.language) {
    case 'en':
        Slg = en;
        break;
    case 'ar':
        Slg = ar;
        break;
    default:
        console.log('error in selected language');
        break;
}
*/